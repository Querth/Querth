# LINE Login channel (https://developers.line.biz/console/)
LINE_CHANNEL_ID=
LINE_CHANNEL_SECRET=

# Random string, at least 32 chars: openssl rand -base64 32
SESSION_SECRET=

# Optional: public base URL if behind a proxy (callback = $APP_URL/api/auth/line/callback)
APP_URL=
